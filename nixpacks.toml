[phases.build]
cmds = [
  "cd web && npm install && npm run build",
  "/app/.venv/bin/pip install -r server/requirements.txt"
]

[start]
cmd = "cd server && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
